<%= form_for(@story) do |f| %>
  <%= f.error_messages %>
  <div class="field">
    <%= f.text_field :title, :placefolder => "Title" %>
  </div>
  <div class="field">
    <%= f.check_box :published %>
    <%= f.label :published, "公開する" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
