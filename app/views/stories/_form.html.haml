= simple_form_for(@story, :html => {:class => 'form-horizontal'}) do |f|
  = f.error_notification
  .form-fields
    = f.input :title, :placefolder => "Title"
    = f.input :published
  .form-actions
    = f.submit :class => "btn btn-primary"
