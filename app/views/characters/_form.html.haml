= simple_form_for(@character, :html => {:class => 'form-horizontal'}) do |f|
  = f.error_notification
  .form-inputs
    = f.input :name, :placeholder => "Character Name", :label => 'Name'
  .form-actions
    = f.button :submit, :class => "btn btn-primary"

