= simple_form_for([@character, @face], :html => {:multipart => true, :class => "form-horizontal"}) do |f|
  = f.error_notification
  .form-fields
    = f.input :name
    = f.input :icon
  .form-actions
    = f.submit :class => "btn btn-primary"
